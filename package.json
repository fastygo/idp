{
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i pkg/authkit/static/css/input.css -o pkg/authkit/static/css/app.css --minify",
    "build:ui8kit-js": "go run ./cmd/gen-ui8kit",
    "build": "npm run build:ui8kit-js && npm run build:css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4"
  }
}
